// const { sum } = require("./mathOperation");
const { sub } = require("./mathOperation");
const { multiply } = require("./mathOperation");
const { sum } = require("./mathOperation");
const { name } = require("./mathOperation");

console.log(name);
console.log(sub(4, 5));
console.log(sum(4, 5));
console.log(multiply(4, 5));
